import { styled as MuiStyled } from '@mui/material';
import React from 'react';

export default function TextContainer() {
  return <Container>TextContainer</Container>;
}

const Container = MuiStyled('div')(({}) => ({}));
