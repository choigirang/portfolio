import { styled } from '@mui/material';
import React from 'react';

export default function SkillsCard() {
  return <Container>SkillsCard</Container>;
}

const Container = styled('div')(({}) => ({
  width: '100%',
  height: 400,
  backgroundColor: '#1A1B24',
  borderRadius: 5,
}));
